[project]
name = "pbmc3k-explorer"
version = "0.1.0"
description = "FastAPI PBMC3k single-cell explorer"
requires-python = ">=3.10"
dependencies = [
    "fastapi",
    "numpy",
    "pandas",
    "scanpy",
    "uvicorn",
]

[tool.uv]
package = false
